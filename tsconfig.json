{
  "//": "ワークスペースに属さないルート直下の TS(E2E とツール設定)を型検査の対象に入れる。各パッケージの型検査は pnpm -r typecheck が担当する",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "types": ["node"]
  },
  "include": ["e2e/**/*.ts", "playwright.config.ts", "vitest.config.ts"]
}
